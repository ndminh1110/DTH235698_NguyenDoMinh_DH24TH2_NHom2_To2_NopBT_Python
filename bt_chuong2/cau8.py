''' Lỗi cú pháp → sai quy tắc viết code.

Lỗi runtime (Exception) → lỗi xảy ra khi chạy (chia 0, sai kiểu, truy cập ngoài phạm vi...).

Lỗi logic → không báo lỗi nhưng kết quả sai.

Cách bắt lỗi → dùng try ... except ... else ... finally. '''